---
id: MIK-010
title: Integrate CLI, event loop, search debounce, persistence, and audio
labels: []
depends_on:
  - MIK-002
  - MIK-005
  - MIK-007
  - MIK-008
  - MIK-009
  - MIK-013
created_at: 2026-06-27T11:01:19Z
updated_at: 2026-06-28T01:09:59Z
---

## Summary

Wire the replacement modules into the executable app: CLI parsing, terminal event loop, native audio runtime, debounced search, persistence, and startup auto-play.

## Context

Source docs:

- `docs/SPEC.md` — Startup Behavior, CLI Options, Search, Keyboard Model
- `docs/superpowers/plans/2026-06-27-radio-replacement.md` — Task 10

## Scope

Implement/replace:

- `src/cli.rs`
- `src/main.rs`
- terminal setup/teardown
- key handling
- audio event polling
- search debounce generation/cancellation
- settings load/save
- startup auto-play behavior

CLI options:

- `--theme <name>`
- `--volume <0-100>`
- `--no-auto-play`
- `--audio-output-device <name>`
- `--low-power`
- optional `--search <query>` if straightforward

## Acceptance Criteria

- First launch with no previous station starts silently with recommendations.
- Normal launch auto-plays previous station unless `--no-auto-play` is passed.
- If previous station fails, app starts/stays usable with recommendations.
- Search runs after 300-500ms debounce and ignores stale results.
- Cached search results are reused.
- Volume/theme/favorites/previous station persist after relevant actions and clean shutdown.
- Keybindings match the spec: Tab, j/k/arrows, Enter, `/`, Space, +/- , f, t, q/Esc.
- Terminal is restored after normal quit and recoverable errors.

## Validation

Run:

```bash
cargo fmt --check
cargo test
cargo check
```

Manual checks:

- `cargo run -- --no-auto-play`
- run, play station, quit, relaunch and confirm previous station auto-play

## Status

- 2026-06-28T00:35:52Z (pi-parent)

- 2026-06-28: Worker implementation reported complete. Starting independent reviewer session. Worker reports CLI/event loop/audio polling/search debounce/persistence/startup auto-play wired, with validations green: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, `cargo check`.

- 2026-06-28T00:48:46Z (pi-parent)

- 2026-06-28: Reviewer verdict APPROVE WITH NOTES. User confirmed reviewer note #1: `--volume <0-100>` should be a one-run startup override, not persisted merely because of clean shutdown. Sending narrow repair to worker; theme override may remain persistent unless changed by separate decision.

## Reports

- 2026-06-28T01:09:09Z (pi-parent)

2026-06-28: Worker repaired reviewer note #1: `--volume <0-100>` is now a one-run startup override. It affects runtime startup/display but is not persisted by clean shutdown or unrelated saves; user `+`/`-` marks volume as changed and persists it. Theme override remains persistent. Reviewer re-review verdict: APPROVE. Parent validation passed: `cargo fmt --check`, `cargo test` (138 lib + 6 audio_spike + 3 binary_entry_path), `cargo check`, `cargo clippy --all-targets -- -D warnings`, marker scan for `ffplay|mpv|Command::new|Color::` in `src/main.rs`, and `lens_diagnostics mode=all severity=error`.

## Status Log

- 2026-06-28T01:09:59Z

Moved from active to completed

Completed after reviewer re-review APPROVE and parent validation passed.
