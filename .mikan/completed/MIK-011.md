---
id: MIK-011
title: Implement ICY metadata demux and Now Playing updates
labels: []
depends_on:
  - MIK-007
  - MIK-008
  - MIK-010
created_at: 2026-06-27T11:01:41Z
updated_at: 2026-06-28T01:34:30Z
---

## Summary

Implement live ICY/Shoutcast metadata extraction and display it in Now Playing when available.

## Context

Source docs:

- `docs/SPEC.md` — Now Playing Metadata
- `docs/audio-spike.md` — ICY parser is helper-tested; full demux remains

## Scope

Implement in `src/audio/icy.rs`, `src/audio/decoder.rs`, `src/audio.rs`, `src/app.rs`, and `src/ui.rs` as needed:

- request `Icy-MetaData: 1` header
- read `icy-metaint` response header
- split audio bytes from metadata blocks
- parse `StreamTitle`
- emit `AudioEvent::IcyTitle`
- update app `now_playing_title` for current station only
- render ICY title above/beside station metadata

## Acceptance Criteria

- Streams without ICY metadata still play normally.
- Streams with `icy-metaint` do not feed metadata bytes into Symphonia as audio.
- Title changes emit events without flooding the app with duplicate unchanged titles.
- App ignores stale title events from stations that are no longer current.
- UI displays title when present and falls back to station name/metadata otherwise.
- Tests cover parser and demux behavior with synthetic byte streams.

## Validation

Run:

```bash
cargo fmt --check
cargo test audio
cargo test app
cargo check
```

Manual check with at least one known ICY station from the curated catalog.

## Status Log

- 2026-06-28T01:13:41Z

Moved from ready to active

Starting implementation after MIK-010 merged in PR #13.

- 2026-06-28T01:34:30Z

Moved from active to completed

Completed after worker implementation, reviewer APPROVE, docs repair, and parent validation passed.

## Status

- 2026-06-28T01:23:38Z (pi-parent)

2026-06-28: Worker implementation reported complete via TDD. Starting independent reviewer session. Worker reports ICY demux/reader, `AudioEvent::IcyTitle`, app `now_playing_title`, UI rendering, audio_spike exhaustive arm, and an AGENTS.md caveat update. Validations reported green: `cargo fmt --check`, `cargo test audio`, `cargo test app`, `cargo test ui`, `cargo test`, `cargo check`, `cargo clippy --all-targets -- -D warnings`.

## Reports

- 2026-06-28T01:31:01Z (pi-parent)

2026-06-28: Reviewer verdict BLOCK solely for stale durable docs. Parent repaired docs-only contradiction: updated `docs/audio-spike.md`, `docs/SPEC.md`, and Task 11 in `docs/superpowers/plans/2026-06-27-radio-replacement.md` to state that `icy-metaint` demux is implemented in `src/audio/icy.rs` and wired through `src/audio/decoder.rs` with synthetic-byte tests; Task 11 implementation steps are marked complete except live manual station verification. Validation after docs repair passed: stale-pending phrase scan, `cargo fmt --check`, `cargo test audio`, `cargo test app`, `cargo test ui`, `cargo check`, and `cargo clippy --all-targets -- -D warnings`.

- 2026-06-28T01:33:35Z (pi-parent)

2026-06-28: Narrow reviewer re-review verdict APPROVE after docs repair. Reviewer verified `docs/audio-spike.md`, `docs/SPEC.md`, and `docs/superpowers/plans/2026-06-27-radio-replacement.md` no longer state that ICY demux is pending; remaining live-station verification note is accurate. No remaining MIK-011 blocker. Non-blocking suggestions retained for future polish: consider clearing `now_playing_title` on Stop/Failed and add an all-metadata-chunk IcyReader test.

- 2026-06-28T01:34:25Z (pi-parent)

2026-06-28: Parent final validation passed after reviewer APPROVE: `cargo fmt --check`, `cargo test` (150 lib + 6 audio_spike + 3 binary_entry_path), `cargo check`, `cargo clippy --all-targets -- -D warnings`, and `lens_diagnostics mode=full severity=error refreshRunners=cached`. Manual live ICY station verification remains listed in the plan as unchecked future/manual verification; automated and reviewer gates are green.
