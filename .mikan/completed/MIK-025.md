---
id: MIK-025
title: Make Spectrum visualizer fill pane width
labels: []
depends_on:
  - MIK-023
metadata:
  kind: slice
  parent: MIK-015
created_at: 2026-06-28T04:44:48Z
updated_at: 2026-06-28T07:56:00Z
---

## Summary

Refactor the existing SpectrumStack renderer so it interpolates/resamples FFT bands to fill the allocated visualizer pane width.

## Scope

- Add a small rendering helper that samples/interpolates band values across `area.width`.
- Update SpectrumStack so it no longer uses only `min(bands.len(), area.width)` columns.
- Preserve current low/mid/high theme color mapping.
- Keep layout unchanged; this is pane-width usage, not a full-width panel redesign.
- Add width-focused regression tests using synthetic `VizFrame` data.

## Acceptance Criteria

- SpectrumStack uses the full visualizer pane width when width exceeds band count.
- Rendering remains safe for tiny widths/heights and empty/silent frames.
- Wide, Medium, and Compact render tests still pass.
- Helper behavior is deterministic and covered by tests.

## Validation

```bash
cargo fmt --check
cargo test ui
cargo test theme
cargo check
```

## Status

- 2026-06-28T07:55:55Z

- 2026-06-28: Updated SpectrumStack rendering to interpolate/resample bands across the full visualizer pane width. Worker session `s_8b88e2b6-4e92-4c45-add1-03e303473357`; reviewer session `s_f9825894-f92a-4cf2-a0e0-cbf63ee3115b`.
- Parent validation: `cargo fmt --check`, `cargo test ui`, `cargo test theme`, `cargo check`, full `cargo test`, `cargo clippy --all-targets -- -D warnings`, and `lens_diagnostics mode=all severity=error` passed.
- Reviewer verdict: APPROVE. Non-blocking note: future narrow-pane behavior may want peak-preserving downsampling when bands exceed width.
