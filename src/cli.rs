//! CLI argument parsing and boundary parsing.
//!
//! Argument definitions and the run entry point are implemented in a later
//! task. Boundary values are parsed into the typed primitives in
//! [`crate::model`] and [`crate::theme`].
